
name: TestMavenBuild

on:
  push:
    branches: [ "master" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: Build
        run: mvn --batch-mode  -Dmaven.test.failure.ignore=true package
